pipeline {
    agent any
    stages {
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t java-hello-pipeline .'
            }
        }
        stage('Run Docker Container') {
            steps {
                sh 'docker run --rm java-hello-pipeline'
            }
        }
    }
}
