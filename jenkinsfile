pipeline {
    agent any
    stages {
        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("java-hello-pipeline")
                }
            }
        }
        stage('Run Docker Container') {
            steps {
                script {
                    docker.image("java-hello-pipeline").run()
                }
            }
        }
    }
}
